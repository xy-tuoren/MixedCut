{
  "name": "mixedcut-electron",
  "version": "1.0.1",
  "description": "Electron app for random video mixing with static ffmpeg.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "prepare:adb:win": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts\\download-adb.ps1",
    "dist:win": "npm run prepare:adb:win && electron-builder --win nsis",
    "pack:win": "npm run prepare:adb:win && electron-builder --win portable"
  },
  "build": {
    "appId": "com.mixedcut.app",
    "productName": "MixedCut",
    "asar": true,
    "asarUnpack": [
      "node_modules/ffmpeg-static/**"
    ],
    "extraResources": [
      {
        "from": "resources/adb/win",
        "to": "adb",
        "filter": [
          "**/*"
        ]
      }
    ],
    "files": [
      "main.js",
      "preload.js",
      "renderer/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        "nsis"
      ],
      "artifactName": "${productName}-Setup-${version}.${ext}"
    }
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3"
  }
}